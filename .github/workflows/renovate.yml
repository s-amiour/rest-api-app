name: Renovate Bot

on:
  schedule:
    - cron: "0 5 * * 1"

  workflow_dispatch:
    inputs:
      log_level:
        description: "Renovate log level"
        required: false
        default: "info"
        type: choice
        options:
          - debug
          - info
          - warn
      ignore_schedule:
        description: "Ignore schedule (open PRs immediately)"
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"

jobs:
  renovate:
    name: Run Renovate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Renovate
        uses: renovatebot/github-action@v41.0.14 # ensure using valid release tag
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
          env-regex: "^(RENOVATE_|LOG_LEVEL)"
        env:
          LOG_LEVEL: ${{ inputs.log_level || 'info' }}
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          RENOVATE_FORCE: ${{ inputs.ignore_schedule == 'true' && '{"schedule":null}' || '{}' }}